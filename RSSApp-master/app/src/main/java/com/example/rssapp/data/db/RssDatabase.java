package com.example.rssapp.data.db;

import android.content.Context;

import androidx.room.Database;
import androidx.room.Room;
import androidx.room.RoomDatabase;

/*
@Database(entities = {
        //entities here
}, version = 1)
public abstract class RssDatabase extends RoomDatabase {
    private static volatile RssDatabase instance = null;

    public static RssDatabase getInstance(Context context) {
        if (instance == null) {
            synchronized (RssDatabase.class) {
                if (instance == null) {
                    instance = Room.databaseBuilder(context.getApplicationContext(),
                            RssDatabase.class, "app_database")
                            .build();
                }
            }
        }
        return instance;
    }
}
*/
